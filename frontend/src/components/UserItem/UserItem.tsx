type Props = {
    users:{
        id:string;
        name:string;
        img:string
    }[]
}
const UserItem = ({ users }:Props)=>{
    return(
        <div>
        </div>
    )
};

export default UserItem;